import { VideoHlsDirective } from './video-hls.directive';

describe('VideoHlsDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoHlsDirective();
    expect(directive).toBeTruthy();
  });
});
